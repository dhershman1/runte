/**
 * Takes a value and turns it into a constant
 * @param {Any} x The value to remain constant
 * @returns {Any} The value provided
 */
function _constant (x) {
  return x
}

module.exports = _constant
